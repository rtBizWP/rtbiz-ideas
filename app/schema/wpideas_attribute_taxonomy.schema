CREATE TABLE %s (
	id BIGINT(20) NOT NULL AUTO_INCREMENT,
	attribute_name VARCHAR(100) NOT NULL,
	attribute_label VARCHAR(100),
	attribute_orderby VARCHAR(100) NOT NULL,
        attribute_post_type VARCHAR(20) NOT NULL,
	PRIMARY KEY  (id)
);